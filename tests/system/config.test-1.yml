# Note: During tests, local loopback/localhost is allowed by the P2P layer
# when INTEGRATION_TEST=true (set by tests). No change needed here.
# Supernode Configuration
supernode:
  key_name: "testkey1"
  identity: "lumera1em87kgrvgttrkvuamtetyaagjrhnu3vjy44at4"
  host: "0.0.0.0"
  port: 4444
  gateway_port: 8002

# Keyring Configuration
keyring:
  backend: "test"  # Options: test, file, os
  dir: "keys"      # Relative to base_dir
  password: "keyring-password"

# P2P Network Configuration
p2p:
  port: 4445
  data_dir: "data/p2p"  # Relative to base_dir
  bootstrap_nodes: "lumera1cf0ms9ttgdvz6zwlqfty4tjcawhuaq69p40w0c@localhost:4447,lumera1cjyc4ruq739e2lakuhargejjkr0q5vg6x3d7kp@localhost:4449"

# Lumera Chain Configuration
lumera:
  grpc_addr: "localhost:9090"
  chain_id: "testing"

# RaptorQ Configuration
raptorq:
  files_dir: "raptorq_files"  # Relative to base_dir
